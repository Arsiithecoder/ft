def ft_plot_area():
    length = int(input("Enter the length of the plot in meters: "))
    width = int(input("Enter the width of the plot in meters: "))
    area = length * width
    print(f"Plot area: {area}")

if __name__ == "__main__":
    ft_plot_area()
